package com.jonlatane.beatpad.util

import java.util.Collections

/*operator fun <T> List<T>.times(n: Int): List<T> {
	return when {
		n < 0 -> throw IllegalArgumentException()
		n == 0 -> emptyList()
		n > 0 -> Collections.nCopies(n, this).flatMap { it }
		else -> throw IllegalStateException("Kotlin whens aren't so great huh.")
	}
}*/
